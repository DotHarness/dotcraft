{
  "name": "acme-review-desktop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run typecheck && npm run build:core && npm run build:consumer",
    "typecheck": "tsc --noEmit",
    "build:core": "dotcraft-plugin build Core",
    "build:consumer": "dotcraft-plugin build Consumer"
  },
  "devDependencies": {
    "@dotcraft/plugin": "0.6.3",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.7.0"
  }
}
