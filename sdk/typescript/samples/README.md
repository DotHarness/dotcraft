# TypeScript SDK samples

- [`applications`](applications) contains focused application examples for the public `@dotcraft/sdk` API.
- [`desktop-plugins`](desktop-plugins) contains installable Desktop Plugin bundles and a local marketplace.

Each directory documents its own build and verification command.
